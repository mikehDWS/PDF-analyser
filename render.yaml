services:
  - type: web
    name: pdf-form-analyzer
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # set this in the Render dashboard, don't commit it
